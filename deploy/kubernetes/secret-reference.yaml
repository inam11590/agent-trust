apiVersion: v1
kind: Secret
metadata:
  name: agenttrust-enrollment-secret
  namespace: agenttrust
type: Opaque
# Secret values should be populated via external secret managers (Vault / AWS Secrets Manager)
stringData:
  enrollment-token: "gw_tok_REPLACE_WITH_ONE_TIME_ENROLLMENT_TOKEN"
